[phases.setup]
nixPkgs = ["python312", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd visa-client && npm ci"
]

[phases.build]
cmds = ["cd visa-client && npm run build"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
