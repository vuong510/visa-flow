# Golden set — ảnh và nhãn KHÔNG commit

Thư mục này chứa ảnh/PDF giấy tờ khách thật cho eval tầng 4 (doc review + OCR): `documents/` và `ocr/`.
Toàn bộ file trong đây bị `.gitignore` chặn commit (PII) — chỉ khung thư mục và tài liệu hướng dẫn được lưu.
Bảng nhãn (theo mẫu `docs/golden-set-template.md`) do chị Yến điền cũng KHÔNG commit: gửi cho dev qua kênh riêng (Zalo/Drive nội bộ), dev giữ trên máy khi chạy eval.
